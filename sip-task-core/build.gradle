apply plugin: 'maven-publish'

publishing {
    publications {
        mavenJava(MavenPublication) {
            groupId 'com.sip'
            artifactId 'sip-task-core'
            version '1.0.0-SNAPSHOT'

            from components.java
            // more goes in here
        }
    }

    repositories {
        mavenLocal()
    }
}
